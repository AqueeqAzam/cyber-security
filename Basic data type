fundamental programming:

1. Simple program 
#include <stdio.h>
int main()                               # function name
{                                        # start of the program
  # program statement or code
  printf("hello world");                 # ptintf function print our code whatever we compute
  return 0;                              # this is return statement
}                                        # end of the function here

2. compute the area of circle
#include <stdio.h>
#include <stdlib.h>
int main()
{
    int r,p;
    r = 5;
    p = 3.14;
    int area = p*r*r;   // if you do not define data type here, then programm can not execute.
    printf("area of circle is %d",area);
    return 0;
}

3. compute a number which is smaller than thousand:
#include <stdio.h>
int main()
{
    int num;
    printf("enter the number\n");
    scanf("%d",&num);
    if (num<1000)
    printf("your number is smaller than thousand\n");
    else
    printf("ypur number is greater than thousand");
    return 0;
}


4. application computing:
#include <stdio.h>
main()
{
  float rice;
  float suger;
  printf("enter the value of price of rise:\n");
  scanf("%f",&rice);
  printf("enter the value of price of suger\n");
  scanf("%f",&suger);
  printf("***** LIST OF ITEMS *****\n");
  printf("item      price\n");
  printf("rice %f",rice);
  printf("suger %f",suger);
  printf("sum of the price of rice and suger is %d %d",suger+rice);
  return 0;

5. compute positive and negative number:  
include <stdio.h>
int main()
{
  int m;
  printf("enter any number:\n");
  scanf("%d",&m);
  if(m > 0)
  printf("your number is positive");
  else
  printf("your number is negative");
  return 0;
}

6. compute table of any number:-
#include <stdio.h>
int main()
{
  int num;
  int i;
  printf("enter the number: ");
  scanf("%d",&num);
  for (i = 1; i <= 10; ++i){
  printf("table of %d x %d = %d \n", num, i, num*i);
  }
  return 0;
  }

6. 




